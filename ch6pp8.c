#include <stdio.h>

int main(void)
{
  int days, start; 

  printf("Enter nummber of days in month: ");
  scanf("%d", &days);
  printf("Enter starting day of the week (1=Sun, 7=Sat): ");
  scanf("%d", &start);

  
}